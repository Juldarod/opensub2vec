import React from 'react';
import { Container, Segment } from 'semantic-ui-react';

const Home = () => {
    return (
        <Container id="hello">
            <Segment>Hello, World!</Segment>
        </Container>
    );
};

export default Home;
